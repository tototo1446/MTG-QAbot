QA×タグ方式で95〜100点出たという実績があるなら、方向性は間違いなく正しいですね。

その上で、現時点で見えている設計上の重要ポイントをお伝えします!
※音声入力による要約になります

①音声→テキスト変換の段階で勝負が決まる
Whisperなどで文字起こしする際、そのまま使うと話者分離ができず「誰が何を言ったか」が消えます。QA形式に変換するなら、話者分離（diarization）は必須です。pyannote.audioやWhisperXを組み合わせると、「話者A: 〜」「話者B: 〜」の形で出せます。ここの精度が低いと、下流のQA生成が全部崩れるので、最初にしっかり作り込むべきところです。

②QA生成のプロンプト設計が肝
MTG音声を丸ごとLLMに投げて「QA化して」だと、粒度がバラバラになります。おそらく既にやられていると思いますが、まず話題単位でチャンク分割→チャンクごとにQA生成、という2段階にしたほうが安定します。1つのMTGから出るQAの数は多いほうがいいです。粒度が細かいほど、検索時のヒット精度が上がります。

③タグ設計は「階層構造＋自由タグ」のハイブリッドがおすすめ
例えばYouTube運用なら、固定階層として「企画 / 撮影 / 編集 / サムネ / タイトル / 分析」のようなカテゴリを持ちつつ、LLMが自動で付与する自由タグ（例：「CTR改善」「冒頭離脱」「コメント誘導」など）を併用する形です。固定タグだけだと網羅性に限界があり、自由タグだけだと表記揺れで検索精度が落ちます。

④検索時は「タグ絞り込み＋ベクトル検索」の併用を
タグで大きく絞った上で、質問文とQAのembedding類似度で最終的にランキングするのがベストです。タグだけだと関連度の順位付けができず、ベクトル検索だけだとノイズが多くなります。両方使うことで、質問に対して本当に関係あるQAだけが上位に来ます。

⑤蓄積量が増えた時のための「QA統合・更新ルール」を最初から決めておく
同じ話題について複数のMTGで異なる結論が出ることがあります。古いQAと新しいQAが矛盾した場合にどちらを優先するか、あるいは統合するかのルールを決めておかないと、蓄積量が増えた時に回答品質が下がります。タイムスタンプを持たせて「最新優先」にするか、明示的に「このQAは更新版です」とメタデータを持たせるか、どちらかの仕組みを入れておくといいです。

⑥横展開を見据えるなら、タグ体系だけ差し替え可能な設計に
YouTube運用のタグ体系と、経営判断やマーケのタグ体系は当然違います。コアのパイプライン（音声→文字起こし→QA生成→タグ付け→蓄積→検索→回答生成）は共通にして、タグのマスターデータだけ差し替えられる設計にしておけば、横展開がスムーズです。

こんな感じで要点をまとめてみました。これらは1番から順に優先順位が高いものになっているので、それぞれ対応していくことで、より精度が高くなるかなと思います。